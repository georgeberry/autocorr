---
title: "scratch"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
source("sim_code.R")
```

```{r}
g_edge = fn_dgp_sampling_edge()
df_edges = as_tibble(g_edge, active='edges')
print(mean(df_edges$gt))
df_nodes = as_tibble(g_edge, active='nodes')
print(mean(df_nodes$gt))
```


```{r}
g_node = fn_dgp_sampling_node()
df_edges = as_tibble(g_node, active='edges')
print(mean(df_edges$gt))
df_nodes = as_tibble(g_node, active='nodes')
print(mean(df_nodes$gt))
```

